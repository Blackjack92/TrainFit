<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TrainFit.Styling">

    <SolidColorBrush x:Key="Grid.Default" Color="#EDF3F5" />
    <SolidColorBrush x:Key="Grid.Header" Color="#80b4ba" />
    <SolidColorBrush x:Key="Grid.Footer" Color="#80b4ba" />

    <SolidColorBrush x:Key="Grid.Background" Color="#EDF3F5" />
    <SolidColorBrush x:Key="TextBox.Foreground" Color="#EDF3F5" />
    <SolidColorBrush x:Key="TextBox.Border" Color="#EDF3F5" />


    <Style x:Key="TextBlock.Base" TargetType="TextBlock">
        <Setter Property="Foreground" Value="#278ea3" />
        <Setter Property="FontSize" Value="23" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Margin" Value="0,0,0,0" />
        <Setter Property="Padding" Value="0,0,0,0" />
    </Style>

    <Style x:Key="TextBlock.Light" TargetType="TextBlock" BasedOn="{StaticResource ResourceKey=TextBlock.Base}">
        <Setter Property="FontWeight" Value="Thin" />
        <Setter Property="Foreground" Value="#55FF55" />
        <Setter Property="FontSize" Value="50" />
    </Style>

    <Style TargetType="TextBlock" BasedOn="{StaticResource TextBlock.Base}" />
</ResourceDictionary>
